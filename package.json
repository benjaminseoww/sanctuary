{
  "name": "sanctuary",
  "version": "1.0.0",
  "description": "Sanctuary monorepo with backend and frontend",
  "private": true,
  "workspaces": [
    "backend",
    "app-frontend"
  ],
  "scripts": {
    "dev:backend": "pnpm --filter backend run dev",
    "dev:frontend": "pnpm --filter app-frontend run dev",
    "lint": "pnpm --filter backend run lint && pnpm --filter app-frontend run lint",
    "typecheck": "pnpm --filter backend run typecheck && pnpm --filter app-frontend run typecheck",
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.0.11"
  }
}